/**
 * @gao/security — Zod Validator Middleware
 *
 * Auto-validates request body, params, and query against Zod schemas.
 * Returns structured validation errors.
 */
import type { Middleware } from '@gao/core';
import { z } from 'zod';
export interface ValidatorOptions<Body extends z.ZodTypeAny = z.ZodTypeAny, Query extends z.ZodTypeAny = z.ZodTypeAny, Params extends z.ZodTypeAny = z.ZodTypeAny> {
    body?: Body;
    query?: Query;
    params?: Params;
    /** Drop unrecognized keys from objects. Default true. */
    stripUnknown?: boolean;
}
/**
 * Zod validation middleware wrapper.
 */
export declare function validate<Body extends z.ZodTypeAny, Query extends z.ZodTypeAny, Params extends z.ZodTypeAny>(options: ValidatorOptions<Body, Query, Params>): Middleware;
//# sourceMappingURL=validator.d.ts.map