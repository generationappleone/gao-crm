/**
 * @gao/orm — MariaDB Driver
 *
 * MariaDB driver implementation using `mariadb`.
 */
import * as mariadb from 'mariadb';
import type { ExecuteResult } from '../types.js';
import type { DatabaseDriver } from './driver.interface.js';
export interface MariaDBDriverOptions extends mariadb.PoolConfig {
}
export declare class MariaDBDriver implements DatabaseDriver {
    private config;
    private pool;
    private connection;
    private isTransaction;
    private savepointDepth;
    constructor(config: MariaDBDriverOptions, connection?: mariadb.Connection, savepointDepth?: number);
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    query<T = unknown>(sql: string, params?: unknown[]): Promise<T[]>;
    execute(sql: string, params?: unknown[]): Promise<ExecuteResult>;
    transaction<T>(callback: (trxDriver: DatabaseDriver) => Promise<T>): Promise<T>;
    getPool(): mariadb.Pool | null;
    private ensureConnected;
}
//# sourceMappingURL=mariadb.d.ts.map