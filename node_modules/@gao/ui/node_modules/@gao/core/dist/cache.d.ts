/**
 * @gao/core — Cache Service
 *
 * In-memory cache with TTL, namespacing, and invalidation.
 * Redis adapter interface for production use.
 * @Cached(ttl) decorator support.
 */
import type { CacheAdapter, CacheOptions } from './types.js';
/**
 * In-memory cache adapter (default — no external dependencies).
 */
export declare class MemoryCacheAdapter implements CacheAdapter {
    private readonly store;
    private cleanupInterval;
    constructor(cleanupIntervalMs?: number);
    get<T>(key: string): Promise<T | undefined>;
    set<T>(key: string, value: T, ttl?: number): Promise<void>;
    delete(key: string): Promise<boolean>;
    clear(namespace?: string): Promise<void>;
    has(key: string): Promise<boolean>;
    get size(): number;
    destroy(): void;
    private cleanup;
    private extractNamespace;
}
/**
 * Cache service with namespace support and decorator.
 */
export declare class CacheService {
    private readonly adapter;
    constructor(adapter?: CacheAdapter);
    get<T>(key: string, options?: CacheOptions): Promise<T | undefined>;
    set<T>(key: string, value: T, options?: CacheOptions): Promise<void>;
    /**
     * Get or set: return cached value, or compute and cache it.
     */
    remember<T>(key: string, factory: () => T | Promise<T>, options?: CacheOptions): Promise<T>;
    delete(key: string, options?: CacheOptions): Promise<boolean>;
    invalidate(namespace: string): Promise<void>;
    clear(): Promise<void>;
    private buildKey;
}
export declare function createCacheService(adapter?: CacheAdapter): CacheService;
//# sourceMappingURL=cache.d.ts.map