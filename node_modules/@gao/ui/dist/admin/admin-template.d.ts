/**
 * @gao/ui — Admin Template Layout Generator
 *
 * Generates complete HTML pages for admin panels using GaoType fonts,
 * GaoIcons, and the GaoAdmin glassmorphism design system.
 */
import type { GaoIconName } from '../icons/icon-types.js';
export interface SidebarItem {
    label: string;
    icon: GaoIconName;
    href?: string;
    active?: boolean;
    badge?: string;
    section?: string;
}
export interface NavbarConfig {
    showSearch?: boolean;
    user?: {
        name: string;
        avatar?: string;
        role?: string;
    };
    notifications?: number;
}
export interface AdminLayoutConfig {
    title: string;
    brandName?: string;
    brandIcon?: GaoIconName;
    sidebar: SidebarItem[];
    navbar?: NavbarConfig;
    content: string;
    footer?: string;
    fonts?: string[];
}
export interface DashboardStat {
    label: string;
    value: string | number;
    icon: GaoIconName;
    trend?: {
        value: string;
        direction: 'up' | 'down';
    };
    color?: string;
}
export interface DashboardConfig extends Omit<AdminLayoutConfig, 'content'> {
    stats: DashboardStat[];
    chartHtml?: string;
    recentTitle?: string;
    recentHtml?: string;
}
export declare const createAdminTemplate: {
    /**
     * Generate the full admin CSS.
     */
    css(): string;
    /**
     * Generate a full admin HTML page.
     */
    layout(config: AdminLayoutConfig): string;
    /**
     * Generate sidebar HTML.
     */
    sidebar(items: SidebarItem[], brandName?: string, brandIcon?: GaoIconName): string;
    /**
     * Generate navbar HTML.
     */
    navbar(config?: NavbarConfig): string;
    /**
     * Generate default footer.
     */
    footer(): string;
};
//# sourceMappingURL=admin-template.d.ts.map