/**
 * @gao/ui — Admin Components
 *
 * Pre-built HTML string generators for admin UI components.
 * All components use GaoIcons and the GaoAdmin CSS design system.
 */
import type { GaoIconName } from '../icons/icon-types.js';
export interface StatCardConfig {
    label: string;
    value: string | number;
    icon: GaoIconName;
    trend?: {
        value: string;
        direction: 'up' | 'down';
    };
    iconColor?: string;
    iconBg?: string;
}
export interface DataTableColumn {
    key: string;
    label: string;
    align?: 'left' | 'center' | 'right';
}
export interface DataTableConfig {
    columns: DataTableColumn[];
    rows: Record<string, string | number>[];
    pageInfo?: {
        current: number;
        total: number;
        perPage: number;
    };
}
export interface ChartDataPoint {
    label: string;
    value: number;
    color?: string;
}
export interface FormField {
    name: string;
    label: string;
    type?: 'text' | 'email' | 'password' | 'number' | 'textarea' | 'select';
    placeholder?: string;
    value?: string;
    options?: {
        label: string;
        value: string;
    }[];
    required?: boolean;
}
export interface ToastConfig {
    message: string;
    type?: 'success' | 'error' | 'warning' | 'info';
    icon?: GaoIconName;
}
export interface ModalConfig {
    id: string;
    title: string;
    body: string;
    footer?: string;
    size?: 'sm' | 'md' | 'lg';
}
export interface BadgeConfig {
    text: string;
    variant?: 'primary' | 'success' | 'warning' | 'danger' | 'info';
    dot?: boolean;
}
export interface ProgressConfig {
    value: number;
    max?: number;
    color?: string;
    showLabel?: boolean;
}
export interface AvatarConfig {
    name: string;
    src?: string;
    size?: 'sm' | 'md' | 'lg';
    status?: boolean;
}
export interface AlertConfig {
    message: string;
    type?: 'success' | 'warning' | 'danger' | 'info';
    icon?: GaoIconName;
}
/**
 * Stat card with icon, value, label, and optional trend.
 */
export declare function statCard(config: StatCardConfig): string;
/**
 * Data table with columns, rows, and optional pagination.
 */
export declare function dataTable(config: DataTableConfig): string;
/**
 * Bar chart using SVG rectangles.
 */
export declare function barChart(data: ChartDataPoint[], height?: number): string;
/**
 * Line chart using SVG polyline.
 */
export declare function lineChart(data: ChartDataPoint[], height?: number): string;
/**
 * Donut chart using SVG circle arcs.
 */
export declare function donutChart(data: ChartDataPoint[], size?: number): string;
/**
 * Form builder from field config.
 */
export declare function form(fields: FormField[], action?: string): string;
/**
 * Breadcrumb navigation.
 */
export declare function breadcrumb(items: {
    label: string;
    href?: string;
}[]): string;
/**
 * Toast notification HTML.
 */
export declare function toast(config: ToastConfig): string;
/**
 * Modal dialog.
 */
export declare function modal(config: ModalConfig): string;
/**
 * Badge component.
 */
export declare function badge(config: BadgeConfig): string;
/**
 * Progress bar.
 */
export declare function progress(config: ProgressConfig): string;
/**
 * Avatar with fallback initials.
 */
export declare function avatar(config: AvatarConfig): string;
/**
 * Empty state placeholder.
 */
export declare function emptyState(title: string, description?: string, icon?: GaoIconName): string;
/**
 * Alert banner.
 */
export declare function alertBanner(config: AlertConfig): string;
//# sourceMappingURL=admin-components.d.ts.map