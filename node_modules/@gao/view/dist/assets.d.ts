/**
 * @gao/view — Asset Pipeline
 *
 * Handles asset path resolution and fingerprinting.
 */
export interface AssetOptions {
    publicPath: string;
    manifestPath?: string;
    isDev?: boolean;
}
export declare class AssetPipeline {
    private options;
    private manifest;
    constructor(options: AssetOptions);
    private loadManifest;
    /**
     * Resolve an asset path.
     */
    resolve(assetPath: string): string;
    /**
     * Helper to generate a fingerprint for a file.
     */
    getFingerprint(filePath: string): string;
}
//# sourceMappingURL=assets.d.ts.map