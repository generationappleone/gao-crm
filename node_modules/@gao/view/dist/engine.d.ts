/**
 * @gao/view — Template Engine
 *
 * Type-safe, high-performance template engine using compiled JS functions.
 * Supports auto-escaping, sections, layouts, and partials.
 */
import type { EngineOptions, RenderData } from './types.js';
export declare class GaoViewEngine {
    private options;
    private fileCache;
    private sections;
    private currentLayout;
    private components;
    private helpers;
    private uiHelpers;
    constructor(options: EngineOptions);
    /**
     * Add UI helpers at runtime (called by plugins like @gao/ui).
     */
    addHelpers(helpers: Record<string, Function>): void;
    /**
     * Escape HTML to prevent XSS.
     */
    private escape;
    /**
     * Render a template string with data.
     */
    renderString(template: string, data?: RenderData): string;
    /**
     * Compile a template string into a render function.
     */
    /**
     * Preprocess control flow directives before tokenization.
     * Converts @if/@foreach/etc. into <@ code @> blocks.
     */
    private preprocessDirectives;
    compile(template: string, filename?: string): (data: RenderData, helpers: any) => string;
    render(viewName: string, data?: RenderData): Promise<string>;
    private renderInternal;
    private createHelpers;
    private resolvePath;
}
//# sourceMappingURL=engine.d.ts.map