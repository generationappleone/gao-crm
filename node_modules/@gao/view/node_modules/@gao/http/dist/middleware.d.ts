/**
 * @gao/http — Middleware Pipeline
 *
 * Onion-style middleware execution pipeline handling asynchronous execution
 * and allowing short-circuiting by returning a Response early.
 */
import type { GaoRequest } from './request.js';
import type { GaoResponse } from './response.js';
export type NextFunction = () => Promise<Response | void>;
export type MiddlewareHandler = (req: GaoRequest, res: GaoResponse, next: NextFunction) => Promise<Response | void> | Response | void;
export declare class MiddlewarePipeline {
    private middlewares;
    /**
     * Add a middleware to the pipeline.
     */
    use(handler: MiddlewareHandler): this;
    /**
     * Executes the pipeline.
     * The `finalHandler` is executed at the center of the onion.
     */
    execute(req: GaoRequest, res: GaoResponse, finalHandler: (req: GaoRequest, res: GaoResponse) => Promise<Response | void> | Response | void): Promise<Response>;
}
//# sourceMappingURL=middleware.d.ts.map