/**
 * @gao/http — Request Validation
 *
 * Zod-based request validation with decorator and imperative APIs.
 */
import { type ZodSchema, type ZodError } from 'zod';
/**
 * Validation error with field-level messages.
 */
export declare class HttpValidationError extends Error {
    readonly statusCode = 422;
    readonly errors: Record<string, string[]>;
    constructor(zodError: ZodError);
    toJSON(): {
        error: {
            code: string;
            message: string;
            errors: Record<string, string[]>;
        };
    };
}
/**
 * Validate data against a Zod schema.
 * Returns typed result or throws HttpValidationError.
 */
export declare function validateData<T>(schema: ZodSchema<T>, data: unknown): T;
/**
 * Metadata key for validation schemas on route handlers.
 */
export declare const VALIDATION_METADATA_KEY: unique symbol;
/**
 * Method decorator to validate request body before handler runs.
 *
 * Usage:
 * ```ts
 * @Post('/contacts')
 * @Validate(CreateContactSchema)
 * async create(req: GaoRequest, res: GaoResponse) {
 *   const data = req.validated<CreateContact>();
 * }
 * ```
 */
export declare function Validate<T>(schema: ZodSchema<T>): (target: any, propertyKey: string, _descriptor?: PropertyDescriptor) => void;
/**
 * Get the validation schema for a specific handler.
 */
export declare function getValidationSchema(target: any, handlerName: string): ZodSchema | undefined;
//# sourceMappingURL=validation.d.ts.map