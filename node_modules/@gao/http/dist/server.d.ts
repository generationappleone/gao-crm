/**
 * @gao/http — Server
 *
 * HTTP Server wrapper favoring Bun.serve() with Node.js 'http' fallback.
 */
import { GaoRequest } from './request.js';
import { GaoResponse } from './response.js';
export interface ServerOptions {
    port?: number;
    hostname?: string;
    tls?: {
        key: string;
        cert: string;
    };
    keepAliveTimeout?: number;
    timeout?: number;
    maxBodySize?: number;
}
export type FetchHandler = (req: GaoRequest, res: GaoResponse) => Promise<Response>;
export declare class Server {
    private handler;
    private options;
    private nodeServer?;
    private bunServer?;
    constructor(handler: FetchHandler, options?: ServerOptions);
    private wrapHandlerWithTimeout;
    listen(): Promise<void>;
    private listenBun;
    private listenNode;
    stop(): Promise<void>;
}
declare global {
    var Bun: any;
}
//# sourceMappingURL=server.d.ts.map